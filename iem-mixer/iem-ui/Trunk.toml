[build]
target = "index.html"
dist = "dist"
# Skip wasm-opt because it doesn't support bulk-memory by default
skip_wasm_opt = true

[watch]
watch = ["src", "index.html", "style.css"]
ignore = ["./target", "./dist"]

[serve]
address = "127.0.0.1"
port = 8080
open = false
