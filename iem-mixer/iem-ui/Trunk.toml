[build]
target = "index.html"
dist = "dist"

# Skip wasm-opt to avoid bulk-memory feature issues in CI
[tools]
wasm_opt = "Off"

[watch]
watch = ["src", "index.html", "style.css"]
ignore = ["./target", "./dist"]

[serve]
address = "127.0.0.1"
port = 8080
open = false
